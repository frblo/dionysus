# Client

Install dependencies:

```sh
npm i
```

Start client in development mode with:

```sh
npm run dev -- --open
```

If you want to try collab remember to do so from different browsers instances.
