# Server

The server is just a simple [y-websocket-server](https://github.com/yjs/y-websocket-server/?tab=readme-ov-file) for the moment.

Install dependencies:

```sh
npm i
```

Start server with:

```sh
npm run yws
```
